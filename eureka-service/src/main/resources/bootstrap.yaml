spring:
  application:
    name: eureka-service
  cloud:
    config:
      fail-fast: true
      retry:
        maxAttempts: 200
        maxInterval: 10000
      enabled: true
      uri: http://config-server-service:8081
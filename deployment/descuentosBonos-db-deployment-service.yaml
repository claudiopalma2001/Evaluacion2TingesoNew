apiVersion: v1
kind: PersistentVolume
metadata:
  name: descuentosBonos-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/descuentosBonos-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: descuentosBonos-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: descuentosBonos-db-deployment
  labels:
    app: descuentosBonos-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: descuentosBonos-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: descuentosBonos-db-deployment
        tier: database
    spec:
      containers:
        - name: descuentosBonos-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_DESCUENTOSBONOS
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_DESCUENTOSBONOS
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: descuentosBonos-db-data
      volumes:
        - name: descuentosBonos-db-data
          persistentVolumeClaim:
            claimName: descuentosBonos-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: descuentosBonos-db-service
  labels:
    app: descuentosBonos-db-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: descuentosBonos-db-deployment
    tier: database
  type: LoadBalancer
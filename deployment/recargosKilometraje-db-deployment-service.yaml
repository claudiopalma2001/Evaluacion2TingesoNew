apiVersion: v1
kind: PersistentVolume
metadata:
  name: recargosKilometraje-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/recargosKilometraje-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recargosKilometraje-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recargosKilometraje-db-deployment
  labels:
    app: recargosKilometraje-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: recargosKilometraje-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: recargosKilometraje-db-deployment
        tier: database
    spec:
      containers:
        - name: recargosKilometraje-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_RECARGOSKILOMETRAJE
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_RECARGOSKILOMETRAJE
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: recargosKilometraje-db-data
      volumes:
        - name: recargosKilometraje-db-data
          persistentVolumeClaim:
            claimName: recargosKilometraje-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: recargosKilometraje-db-service
  labels:
    app: recargosKilometraje-db-service
    tier: database
spec:
  ports:
    - port: 5438
      targetPort: 5432
      protocol: TCP
  selector:
    app: recargosKilometraje-db-deployment
    tier: database
  type: LoadBalancer
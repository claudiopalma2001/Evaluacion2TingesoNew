apiVersion: v1
kind: PersistentVolume
metadata:
  name: descuentosCantidadReparacion-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/descuentosCantidadReparacion-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: descuentosCantidadReparacion-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: descuentosCantidadReparacion-db-deployment
  labels:
    app: descuentosCantidadReparacion-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: descuentosCantidadReparacion-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: descuentosCantidadReparacion-db-deployment
        tier: database
    spec:
      containers:
        - name: descuentosCantidadReparacion-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_DESCUENTOSCANTIDADREPARACION
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_DESCUENTOSCANTIDADREPARACION
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: descuentosCantidadReparacion-db-data
      volumes:
        - name: descuentosCantidadReparacion-db-data
          persistentVolumeClaim:
            claimName: descuentosCantidadReparacion-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: descuentosCantidadReparacion-db-service
  labels:
    app: descuentosCantidadReparacion-db-service
    tier: database
spec:
  ports:
    - port: 5433
      targetPort: 5432
      protocol: TCP
  selector:
    app: descuentosCantidadReparacion-db-deployment
    tier: database
  type: LoadBalancer
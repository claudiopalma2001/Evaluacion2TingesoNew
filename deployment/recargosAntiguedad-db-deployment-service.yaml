apiVersion: v1
kind: PersistentVolume
metadata:
  name: recargosAntiguedad-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/recargosAntiguedad-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recargosAntiguedad-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recargosAntiguedad-db-deployment
  labels:
    app: recargosAntiguedad-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: recargosAntiguedad-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: recargosAntiguedad-db-deployment
        tier: database
    spec:
      containers:
        - name: recargosAntiguedad-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_RECARGOSANTIGUEDAD
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_RECARGOSANTIGUEDAD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: recargosAntiguedad-db-data
      volumes:
        - name: recargosAntiguedad-db-data
          persistentVolumeClaim:
            claimName: recargosAntiguedad-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: recargosAntiguedad-db-service
  labels:
    app: recargosAntiguedad-db-service
    tier: database
spec:
  ports:
    - port: 5437
      targetPort: 5432
      protocol: TCP
  selector:
    app: recargosAntiguedad-db-deployment
    tier: database
  type: LoadBalancer
server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/claudiopalma2001/Evaluacion2TingesoNew
          search-paths: config-data
  application:
    name: config-service
